// Projects are a team feature (see PLAN.md §M4). M0 renders a static placeholder;
// this hook is the seam where the projects list read (a useQuery over the
// projects API + decrypted project vaults) will live.
export function useProjectsLogic() {
  return {};
}
